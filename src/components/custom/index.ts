/**
 * Custom Components
 *
 * This folder contains custom, reusable components specific to Brains.
 * These components are built on top of base UI components and include
 * application-specific logic and theming.
 */

export { ConfirmDialog } from './ConfirmDialog';
